@model UniVerse.Web.Models.DashboardViewModel
@{
    Layout = null; // Immersive full-screen dashboard matching target screenshot 100%
    ViewData["Title"] = "Dashboard · UniVerse";
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"]</title>

    <!-- Google Fonts: Inter & Space Mono -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&family=Space+Mono:ital,wght@0,400;0,700;1,400&display=swap" rel="stylesheet" />

    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet" />

    <style>
        :root {
            --uv-bg: #030708;
            --uv-card-bg: rgba(9, 16, 13, 0.62);
            --uv-card-border: rgba(255, 255, 255, 0.055);
            --uv-card-border-hover: rgba(0, 229, 153, 0.22);
            --uv-emerald: #00e599;
            --uv-emerald-glow: rgba(0, 229, 153, 0.35);
            --uv-text-main: #ffffff;
            --uv-text-muted: #83978d;
            --uv-text-dim: #54685f;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            background-color: var(--uv-bg);
            color: var(--uv-text-main);
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
            min-height: 100vh;
            min-height: 100dvh;
            overflow-x: hidden;
            position: relative;
        }

        /* ── Canvas Starfield & Ambient Cosmic Nebula ── */
        #starCanvas {
            position: fixed;
            inset: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 0;
        }

        .ambient-glow {
            position: fixed;
            pointer-events: none;
            z-index: 1;
            filter: blur(140px);
            opacity: 0.55;
        }

        .glow-top-center {
            top: -120px;
            left: 50%;
            transform: translateX(-50%);
            width: 500px;
            height: 500px;
            background: radial-gradient(circle, rgba(0, 229, 153, 0.14) 0%, transparent 70%);
        }

        /* ── Top Navbar (Matching Screenshot: Brand Left, Hamburger Right ONLY) ── */
        .dash-nav {
            position: sticky;
            top: 0;
            z-index: 50;
            width: 100%;
            background: rgba(3, 7, 8, 0.88);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.04);
            padding: 0.75rem 1rem;
        }

        .nav-inner {
            width: 100%;
            max-width: 480px;
            margin: 0 auto;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .nav-brand {
            display: flex;
            align-items: center;
            gap: 0.55rem;
            text-decoration: none;
        }

        .brand-monogram {
            width: 28px;
            height: 28px;
            border-radius: 50%;
            background: rgba(0, 229, 153, 0.1);
            border: 1.5px solid var(--uv-emerald);
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 0 12px var(--uv-emerald-glow);
        }

        .brand-monogram svg {
            width: 14px;
            height: 14px;
        }

        .brand-name {
            font-size: 1.15rem;
            font-weight: 800;
            color: #ffffff;
            letter-spacing: -0.2px;
        }

        .btn-hamburger {
            width: 36px;
            height: 36px;
            border-radius: 9px;
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.08);
            color: rgba(255, 255, 255, 0.8);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.15rem;
            cursor: pointer;
            transition: all 0.2s ease;
        }

        .btn-hamburger:hover {
            border-color: rgba(0, 229, 153, 0.35);
            color: #ffffff;
            background: rgba(0, 229, 153, 0.06);
        }

        /* ── Centered Column Main Container (480px Matching Screenshot Proportions) ── */
        .dash-container {
            position: relative;
            z-index: 10;
            width: 100%;
            max-width: 480px;
            margin: 0 auto;
            padding: 1.1rem 0.85rem 3.5rem;
            display: flex;
            flex-direction: column;
            gap: 0.85rem;
        }

        /* ── Hero Header Section ── */
        .dash-hero-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            gap: 0.6rem;
            margin-bottom: 0.4rem;
            position: relative;
        }

        .hero-left {
            flex: 1;
            min-width: 0;
        }

        .greeting-pill {
            display: inline-flex;
            align-items: center;
            gap: 0.35rem;
            padding: 0.22rem 0.7rem;
            border-radius: 9999px;
            background: rgba(0, 229, 153, 0.08);
            border: 1px solid rgba(0, 229, 153, 0.22);
            color: var(--uv-emerald);
            font-size: 0.68rem;
            font-weight: 700;
            margin-bottom: 0.55rem;
        }

        .welcome-title {
            font-size: 1.35rem;
            font-weight: 800;
            letter-spacing: -0.3px;
            line-height: 1.25;
            color: #ffffff;
            word-break: break-word;
        }

        .student-name-highlight {
            color: var(--uv-emerald);
        }

        .welcome-sub {
            font-size: 0.68rem;
            color: var(--uv-text-muted);
            margin-top: 0.35rem;
            line-height: 1.35;
        }

        .welcome-sub .dot-sep {
            color: rgba(0, 229, 153, 0.45);
            margin: 0 0.25rem;
        }

        .hero-right {
            display: flex;
            flex-direction: column;
            align-items: flex-end;
            gap: 0.65rem;
            flex-shrink: 0;
            padding-top: 0.2rem;
        }

        .hero-actions-row {
            display: flex;
            align-items: center;
            gap: 0.45rem;
        }

        .btn-bell {
            width: 34px;
            height: 34px;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.09);
            color: rgba(255, 255, 255, 0.8);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 0.95rem;
            cursor: pointer;
            transition: all 0.2s ease;
        }

        .btn-bell:hover {
            border-color: rgba(0, 229, 153, 0.35);
            color: #ffffff;
            background: rgba(0, 229, 153, 0.08);
        }

        .btn-new-request-pill {
            display: inline-flex;
            align-items: center;
            gap: 0.3rem;
            background: var(--uv-emerald);
            color: #020806;
            font-size: 0.72rem;
            font-weight: 800;
            padding: 0.45rem 0.85rem;
            border-radius: 9999px;
            border: none;
            cursor: pointer;
            text-decoration: none;
            transition: all 0.2s ease;
            box-shadow: 0 0 14px var(--uv-emerald-glow);
        }

        .btn-new-request-pill:hover {
            background: #25f4ab;
            transform: translateY(-1px);
        }

        /* The glowing 3D planet orb floating beneath '+ New Request' */
        .hero-planet-wrapper {
            margin-right: 0.25rem;
            margin-top: 0.15rem;
        }

        .header-planet-orb {
            width: 36px;
            height: 36px;
            border-radius: 50%;
            background: radial-gradient(circle at 35% 30%, #00f5a0 0%, #00a86b 40%, #013220 85%, #00120b 100%);
            box-shadow: 0 0 24px rgba(0, 229, 153, 0.5), inset 0 0 8px rgba(0, 229, 153, 0.35);
            animation: planetFloat 4s ease-in-out infinite alternate;
        }

        @@keyframes planetFloat {
            0% { transform: translateY(0px) rotate(0deg); }
            100% { transform: translateY(-4px) rotate(8deg); }
        }

        /* ── 2x2 Stats Grid Matching Screenshot ── */
        .stats-grid-2x2 {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 0.65rem;
        }

        .stat-card-box {
            background: var(--uv-card-bg);
            border: 1px solid var(--uv-card-border);
            border-radius: 14px;
            padding: 0.85rem 1rem;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            min-height: 92px;
            position: relative;
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            transition: all 0.2s ease;
        }

        .stat-card-box:hover {
            border-color: var(--uv-card-border-hover);
            transform: translateY(-1px);
        }

        .stat-card-top {
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .stat-label-mono {
            font-size: 0.58rem;
            font-weight: 700;
            letter-spacing: 0.08em;
            text-transform: uppercase;
            color: #7f9689;
            font-family: 'Space Mono', monospace;
        }

        .stat-icon-circle {
            width: 26px;
            height: 26px;
            border-radius: 50%;
            background: rgba(0, 229, 153, 0.08);
            border: 1px solid rgba(0, 229, 153, 0.2);
            color: var(--uv-emerald);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 0.78rem;
        }

        .stat-number-mono {
            font-family: 'Space Mono', monospace;
            font-size: 1.6rem;
            font-weight: 700;
            color: #ffffff;
            line-height: 1.1;
            margin: 0.2rem 0 0.1rem;
        }

        .stat-trend-text {
            font-size: 0.64rem;
            color: #5a7065;
        }

        /* ── Standard Section Cards ── */
        .dash-card-section {
            background: var(--uv-card-bg);
            border: 1px solid var(--uv-card-border);
            border-radius: 14px;
            padding: 1rem 1.05rem;
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            transition: border-color 0.2s ease;
        }

        .dash-card-section:hover {
            border-color: var(--uv-card-border-hover);
        }

        .section-header-row {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 0.65rem;
        }

        .section-header-title {
            font-size: 0.88rem;
            font-weight: 800;
            color: #ffffff;
            display: flex;
            align-items: center;
            gap: 0.4rem;
        }

        .badge-count-pill {
            background: var(--uv-emerald);
            color: #030708;
            font-size: 0.62rem;
            font-weight: 800;
            padding: 0.08rem 0.45rem;
            border-radius: 9999px;
            margin-left: 0.25rem;
        }

        .empty-placeholder {
            text-align: center;
            padding: 1.75rem 0.5rem;
            color: #6b8075;
            font-size: 0.72rem;
            font-weight: 500;
            line-height: 1.4;
        }

        /* ── Overview Analytics ── */
        .analytics-subtext {
            font-size: 0.64rem;
            color: #6b8075;
            margin-top: 0.15rem;
        }

        .pill-select-btn {
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.08);
            border-radius: 9999px;
            padding: 0.25rem 0.65rem;
            color: #ffffff;
            font-size: 0.66rem;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 0.3rem;
            cursor: pointer;
        }

        .chart-box-relative {
            margin-top: 1rem;
            position: relative;
            height: 80px;
            width: 100%;
        }

        .chart-y-axis-labels {
            position: absolute;
            left: 0;
            top: 0;
            bottom: 20px;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            font-family: 'Space Mono', monospace;
            font-size: 0.58rem;
            color: #5a7065;
        }

        .chart-stage-area {
            margin-left: 18px;
            height: 100%;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        .chart-svg-draw {
            flex: 1;
            width: 100%;
            position: relative;
        }

        .chart-svg-draw svg {
            width: 100%;
            height: 100%;
            overflow: visible;
        }

        .chart-x-days-row {
            display: flex;
            justify-content: space-between;
            padding-top: 0.35rem;
            font-family: 'Space Mono', monospace;
            font-size: 0.58rem;
            color: #6b8075;
        }

        /* ── Requests by Status ── */
        .badge-total-pill {
            border: 1px solid rgba(0, 229, 153, 0.35);
            color: var(--uv-emerald);
            font-size: 0.62rem;
            font-weight: 700;
            padding: 0.1rem 0.5rem;
            border-radius: 9999px;
            background: rgba(0, 229, 153, 0.06);
        }

        .status-chart-flex {
            display: flex;
            align-items: center;
            gap: 1.25rem;
            margin-top: 0.3rem;
        }

        .status-donut-ring {
            width: 76px;
            height: 76px;
            position: relative;
            flex-shrink: 0;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .status-donut-ring svg {
            transform: rotate(-90deg);
            width: 100%;
            height: 100%;
        }

        .donut-center-info {
            position: absolute;
            text-align: center;
        }

        .donut-val-mono {
            font-family: 'Space Mono', monospace;
            font-size: 1.25rem;
            font-weight: 700;
            color: #ffffff;
            line-height: 1;
        }

        .donut-tag-mono {
            font-size: 0.52rem;
            font-family: 'Space Mono', monospace;
            color: #5a7065;
            letter-spacing: 0.08em;
            text-transform: uppercase;
            margin-top: 0.12rem;
        }

        .status-rows-stack {
            flex: 1;
            display: flex;
            flex-direction: column;
            gap: 0.35rem;
        }

        .status-pill-item {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0.42rem 0.65rem;
            background: rgba(255, 255, 255, 0.015);
            border: 1px solid rgba(255, 255, 255, 0.03);
            border-radius: 6px;
        }

        .status-item-left {
            display: flex;
            align-items: center;
            gap: 0.45rem;
            font-size: 0.72rem;
            font-weight: 600;
            color: rgba(255, 255, 255, 0.85);
        }

        .status-dot-circle {
            width: 6px;
            height: 6px;
            border-radius: 50%;
        }

        .dot-c-green { background: #00e599; box-shadow: 0 0 5px #00e599; }
        .dot-c-amber { background: #f59e0b; box-shadow: 0 0 5px #f59e0b; }
        .dot-c-red { background: #ef4444; box-shadow: 0 0 5px #ef4444; }

        .status-pct-label {
            font-family: 'Space Mono', monospace;
            font-size: 0.68rem;
            font-weight: 700;
            color: #6b8075;
        }

        /* ── Recent Completed Table ── */
        .table-headers-grid {
            display: grid;
            grid-template-columns: 2fr 1.5fr 1.5fr 1.2fr 1fr 1fr 1fr;
            padding: 0.4rem 0.2rem 0.5rem;
            border-bottom: 1px solid rgba(255, 255, 255, 0.04);
            font-family: 'Space Mono', monospace;
            font-size: 0.56rem;
            font-weight: 700;
            color: #6b8075;
            letter-spacing: 0.06em;
            text-transform: uppercase;
        }

        /* ── Quick Actions ── */
        .quick-actions-box {
            display: flex;
            flex-direction: column;
            gap: 0.65rem;
        }

        .btn-create-request-big {
            width: 100%;
            background: linear-gradient(90deg, #00e599 0%, #00d284 100%);
            color: #020806;
            font-size: 0.78rem;
            font-weight: 800;
            padding: 0.65rem 1rem;
            border-radius: 10px;
            border: none;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.4rem;
            cursor: pointer;
            text-decoration: none;
            transition: all 0.2s ease;
            box-shadow: 0 0 16px var(--uv-emerald-glow);
        }

        .btn-create-request-big:hover {
            background: #25f4ab;
            transform: translateY(-1px);
        }

        .shortcut-item-row {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0.55rem 0.65rem;
            border-radius: 8px;
            background: rgba(255, 255, 255, 0.015);
            border: 1px solid rgba(255, 255, 255, 0.03);
            color: #d0dcd5;
            text-decoration: none;
            font-size: 0.74rem;
            font-weight: 600;
            transition: all 0.2s ease;
        }

        .shortcut-item-row:hover {
            background: rgba(0, 229, 153, 0.06);
            border-color: rgba(0, 229, 153, 0.2);
            color: #ffffff;
            transform: translateX(2px);
        }

        .shortcut-item-left {
            display: flex;
            align-items: center;
            gap: 0.55rem;
        }

        .shortcut-item-left i {
            color: var(--uv-emerald);
            font-size: 0.85rem;
        }

        .shortcut-chevron {
            color: #4a5c53;
            font-size: 0.7rem;
        }

        /* ── Profile Summary Card ── */
        .profile-summary-box {
            display: flex;
            flex-direction: column;
            gap: 0.65rem;
        }

        .profile-top-avatar-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
        }

        .avatar-initial-box {
            width: 36px;
            height: 36px;
            border-radius: 8px;
            background: var(--uv-emerald);
            color: #020806;
            font-size: 1.1rem;
            font-weight: 900;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 0 12px rgba(0, 229, 153, 0.35);
            flex-shrink: 0;
        }

        .profile-identity-info {
            display: flex;
            flex-direction: column;
            gap: 0.15rem;
        }

        .student-full-name {
            font-size: 0.82rem;
            font-weight: 800;
            color: #ffffff;
            line-height: 1.1;
        }

        .student-verified-pill {
            display: inline-flex;
            align-items: center;
            gap: 0.25rem;
            font-size: 0.58rem;
            font-weight: 700;
            color: var(--uv-emerald);
            background: rgba(0, 229, 153, 0.08);
            border: 1px solid rgba(0, 229, 153, 0.22);
            padding: 0.06rem 0.38rem;
            border-radius: 9999px;
            width: fit-content;
        }

        .profile-contact-data {
            display: flex;
            flex-direction: column;
            gap: 0.3rem;
            padding-top: 0.5rem;
            border-top: 1px solid rgba(255, 255, 255, 0.04);
            font-size: 0.68rem;
            color: #7f9689;
        }

        .contact-data-line {
            display: flex;
            align-items: center;
            gap: 0.4rem;
        }

        .contact-data-line i {
            color: var(--uv-emerald);
            font-size: 0.78rem;
        }

        .btn-edit-student-profile {
            width: 100%;
            background: transparent;
            border: 1px solid rgba(0, 229, 153, 0.28);
            border-radius: 8px;
            padding: 0.45rem;
            color: var(--uv-emerald);
            font-size: 0.72rem;
            font-weight: 700;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.35rem;
            cursor: pointer;
            text-decoration: none;
            transition: all 0.2s ease;
            margin-top: 0.15rem;
        }

        .btn-edit-student-profile:hover {
            background: rgba(0, 229, 153, 0.08);
            border-color: var(--uv-emerald);
        }
    </style>
</head>
<body>
    <!-- ── Interactive Canvas Twinkling Stars Background ── -->
    <canvas id="starCanvas"></canvas>

    <!-- ── Ambient Cosmic Glows ── -->
    <div class="ambient-glow glow-top-center"></div>

    <!-- ── Top Navbar (Brand Left, Hamburger Right) ── -->
    <header class="dash-nav">
        <div class="nav-inner">
            <a asp-controller="Home" asp-action="Index" class="nav-brand">
                <div class="brand-monogram">
                    <svg viewBox="0 0 28 28" fill="none" xmlns="http://www.w3.org/2000/svg">
                        <path d="M7 6V14C7 17.866 10.134 21 14 21C17.866 21 21 17.866 21 14V6" stroke="white" stroke-width="2.8" stroke-linecap="round"/>
                        <circle cx="14" cy="14" r="2.2" fill="#00e599" />
                    </svg>
                </div>
                <span class="brand-name">UniVerse</span>
            </a>

            <!-- Only Hamburger Menu Icon Button on Right (Matching Screenshot) -->
            <button class="btn-hamburger" onclick="toggleNavMenu()" aria-label="Menu">
                <i class="bi bi-list"></i>
            </button>
        </div>
    </header>

    <!-- ── Main Dashboard Single-Column Stack (Matching Screenshot Proportions) ── -->
    <main class="dash-container">
        <!-- ── 1. Hero Header Section ── -->
        <div class="dash-hero-header">
            <!-- Left Side: Greeting, Welcome Back, Subtitle -->
            <div class="hero-left">
                <!-- Greeting Pill -->
                <div class="greeting-pill">
                    <span id="greetingText">Good night 🌙</span>
                </div>

                <!-- Welcome Title -->
                <h1 class="welcome-title">
                    <span>Welcome back,</span>
                    <span class="student-name-highlight">@Model.DisplayName</span>
                    <span>👋</span>
                </h1>

                <!-- Subtitle -->
                <p class="welcome-sub">
                    @Model.UniversityName <span class="dot-sep">•</span> Your campus deliveries, your UniVerse.
                </p>
            </div>

            <!-- Right Side: Bell Icon, + New Request, Glowing Green Planet Orb -->
            <div class="hero-right">
                <div class="hero-actions-row">
                    <!-- Bell Icon Button -->
                    <button class="btn-bell" onclick="showNotice('Notifications', 'No new campus announcements right now.')" aria-label="Notifications">
                        <i class="bi bi-bell"></i>
                    </button>

                    <!-- New Request Pill Button -->
                    <a asp-controller="Dashboard" asp-action="NewRequest" class="btn-new-request-pill">
                        <i class="bi bi-plus-lg"></i>
                        <span>New Request</span>
                    </a>
                </div>

                <!-- Floating Green Planet Orb Beneath Buttons -->
                <div class="hero-planet-wrapper">
                    <div class="header-planet-orb"></div>
                </div>
            </div>
        </div>

        <!-- ── 2. 2x2 Stats Grid Matching Screenshot ── -->
        <div class="stats-grid-2x2">
            <!-- Stat 1: Total Requests -->
            <div class="stat-card-box">
                <div class="stat-card-top">
                    <span class="stat-label-mono">TOTAL REQUESTS</span>
                    <div class="stat-icon-circle">
                        <i class="bi bi-box-seam"></i>
                    </div>
                </div>
                <div>
                    <div class="stat-number-mono">@Model.TotalRequests</div>
                    <div class="stat-trend-text">All time</div>
                </div>
            </div>

            <!-- Stat 2: Active Requests -->
            <div class="stat-card-box">
                <div class="stat-card-top">
                    <span class="stat-label-mono">ACTIVE REQUESTS</span>
                    <div class="stat-icon-circle">
                        <i class="bi bi-bicycle"></i>
                    </div>
                </div>
                <div>
                    <div class="stat-number-mono">@Model.ActiveRequests</div>
                    <div class="stat-trend-text">In progress</div>
                </div>
            </div>

            <!-- Stat 3: Completed -->
            <div class="stat-card-box">
                <div class="stat-card-top">
                    <span class="stat-label-mono">COMPLETED</span>
                    <div class="stat-icon-circle">
                        <i class="bi bi-check2-circle"></i>
                    </div>
                </div>
                <div>
                    <div class="stat-number-mono">@Model.CompletedRequests</div>
                    <div class="stat-trend-text">Delivered</div>
                </div>
            </div>

            <!-- Stat 4: Cancelled -->
            <div class="stat-card-box">
                <div class="stat-card-top">
                    <span class="stat-label-mono">CANCELLED</span>
                    <div class="stat-icon-circle">
                        <i class="bi bi-exclamation-circle"></i>
                    </div>
                </div>
                <div>
                    <div class="stat-number-mono">@Model.CancelledRequests</div>
                    <div class="stat-trend-text">All clean · 0 issues</div>
                </div>
            </div>
        </div>

        <!-- ── 3. Active Deliveries Section ── -->
        <section class="dash-card-section">
            <div class="section-header-row">
                <div class="section-header-title">
                    <span>Active Deliveries</span>
                    <span class="badge-count-pill">@Model.ActiveRequests</span>
                </div>
            </div>

            @if (Model.ActiveDeliveries.Count == 0)
            {
                <div class="empty-placeholder">
                    No active deliveries right now. Tap &ldquo;New Request&rdquo; to order snacks!
                </div>
            }
            else
            {
                <div style="display: flex; flex-direction: column; gap: 0.65rem;">
                    @foreach (var req in Model.ActiveDeliveries)
                    {
                        <div style="display: flex; justify-content: space-between; align-items: center; padding: 0.65rem; border-radius: 8px; background: rgba(255,255,255,0.02); border: 1px solid rgba(255,255,255,0.04);">
                            <div>
                                <b style="color: #ffffff; font-size: 0.82rem;">@req.ItemsDescription</b>
                                <div style="color: var(--uv-text-dim); font-size: 0.68rem;">To: @req.HostelRoom</div>
                            </div>
                            <div style="text-align: right;">
                                <span style="color: #f59e0b; font-size: 0.68rem; font-weight: 700;">● @req.Status</span>
                                <div style="color: var(--uv-emerald); font-weight: 800; font-family: 'Space Mono', monospace; font-size: 0.85rem;">₹@req.TotalAmount</div>
                            </div>
                        </div>
                    }
                </div>
            }
        </section>

        <!-- ── 4. Overview Analytics Section ── -->
        <section class="dash-card-section">
            <div class="section-header-row">
                <div>
                    <div class="section-header-title" style="margin-bottom: 0;">Overview Analytics</div>
                    <div class="analytics-subtext">Campus delivery activity this week</div>
                </div>
                <button class="pill-select-btn">
                    <span>This Week</span>
                    <i class="bi bi-chevron-down" style="font-size: 0.58rem;"></i>
                </button>
            </div>

            <!-- Sparkline Chart Area -->
            <div class="chart-box-relative">
                <!-- Y-axis -->
                <div class="chart-y-axis-labels">
                    <span>1</span>
                    <span>0</span>
                </div>

                <!-- Chart SVG Drawing -->
                <div class="chart-stage-area">
                    <div class="chart-svg-draw">
                        <svg viewBox="0 0 700 70" preserveAspectRatio="none">
                            <!-- Dotted Upper Grid line -->
                            <line x1="0" y1="8" x2="700" y2="8" stroke="rgba(255, 255, 255, 0.04)" stroke-dasharray="3,3" />
                            <!-- Baseline Solid line -->
                            <line x1="0" y1="65" x2="700" y2="65" stroke="rgba(255, 255, 255, 0.05)" />

                            <!-- Sleek glowing green sparkline line matching screenshot -->
                            <path d="M 10 65 L 115 65 L 225 65 L 340 65 L 450 65 L 565 65 L 690 65"
                                  fill="none"
                                  stroke="#00e599"
                                  stroke-width="2.2"
                                  stroke-linecap="round"
                                  style="filter: drop-shadow(0 0 5px rgba(0, 229, 153, 0.75));" />
                        </svg>
                    </div>

                    <!-- X-axis Days -->
                    <div class="chart-x-days-row">
                        <span>Mon</span>
                        <span>Tue</span>
                        <span>Wed</span>
                        <span>Thu</span>
                        <span>Fri</span>
                        <span>Sat</span>
                        <span>Sun</span>
                    </div>
                </div>
            </div>
        </section>

        <!-- ── 5. Requests by Status Section ── -->
        <section class="dash-card-section">
            <div class="section-header-row">
                <div class="section-header-title">Requests by Status</div>
                <span class="badge-total-pill">@Model.TotalRequests Total</span>
            </div>

            <div class="status-chart-flex">
                <!-- Left Donut Ring Chart -->
                <div class="status-donut-ring">
                    <svg viewBox="0 0 36 36">
                        <path stroke="rgba(255, 255, 255, 0.06)" stroke-width="3.2" fill="none"
                              d="M18 2.0845 a 15.9155 15.9155 0 0 1 0 31.831 a 15.9155 15.9155 0 0 1 0 -31.831" />
                        <path stroke="#00e599" stroke-width="3.2" stroke-linecap="round" fill="none"
                              stroke-dasharray="@Model.CompletedPercentage, 100"
                              style="filter: drop-shadow(0 0 5px rgba(0, 229, 153, 0.6));"
                              d="M18 2.0845 a 15.9155 15.9155 0 0 1 0 31.831 a 15.9155 15.9155 0 0 1 0 -31.831" />
                    </svg>
                    <div class="donut-center-info">
                        <div class="donut-val-mono">@Model.TotalRequests</div>
                        <div class="donut-tag-mono">TOTAL</div>
                    </div>
                </div>

                <!-- Right Status Breakdown Rows -->
                <div class="status-rows-stack">
                    <div class="status-pill-item">
                        <div class="status-item-left">
                            <span class="status-dot-circle dot-c-green"></span>
                            <span>Completed</span>
                        </div>
                        <span class="status-pct-label">@Model.CompletedPercentage%</span>
                    </div>

                    <div class="status-pill-item">
                        <div class="status-item-left">
                            <span class="status-dot-circle dot-c-amber"></span>
                            <span>In Progress</span>
                        </div>
                        <span class="status-pct-label">@Model.InProgressPercentage%</span>
                    </div>

                    <div class="status-pill-item">
                        <div class="status-item-left">
                            <span class="status-dot-circle dot-c-red"></span>
                            <span>Cancelled</span>
                        </div>
                        <span class="status-pct-label">@Model.CancelledPercentage%</span>
                    </div>
                </div>
            </div>
        </section>

        <!-- ── 6. Recent Completed Section ── -->
        <section class="dash-card-section" style="overflow-x: auto;">
            <div class="section-header-row" style="margin-bottom: 0.35rem;">
                <div class="section-header-title">Recent Completed</div>
            </div>

            <div style="width: 100%; min-width: 440px;">
                <!-- Table Headers -->
                <div class="table-headers-grid">
                    <span>ITEM</span>
                    <span>FROM</span>
                    <span>TO</span>
                    <span>RUNNER</span>
                    <span>TIME</span>
                    <span>STATUS</span>
                    <span style="text-align: right;">PRICE</span>
                </div>

                @if (Model.RecentCompleted.Count == 0)
                {
                    <div class="empty-placeholder" style="padding: 2.25rem 0.5rem;">
                        No completed requests yet.
                    </div>
                }
                else
                {
                    <div style="display: flex; flex-direction: column;">
                        @foreach (var item in Model.RecentCompleted)
                        {
                            <div style="display: grid; grid-template-columns: 2fr 1.5fr 1.5fr 1.2fr 1fr 1fr 1fr; padding: 0.65rem 0.2rem; border-bottom: 1px solid rgba(255,255,255,0.035); font-size: 0.72rem; align-items: center;">
                                <b style="color: #ffffff;">@item.ItemsDescription</b>
                                <span style="color: var(--uv-text-muted);">@item.PickupLocation</span>
                                <span style="color: var(--uv-text-muted);">@item.DropoffLocation</span>
                                <span style="color: #ffffff;">@(item.RunnerName ?? "Assigned")</span>
                                <span style="color: var(--uv-text-dim); font-family: 'Space Mono', monospace; font-size: 0.65rem;">@item.CreatedAt.ToString("MMM dd")</span>
                                <span><span style="background: rgba(0,229,153,0.1); color: var(--uv-emerald); padding: 0.1rem 0.4rem; border-radius: 9999px; font-size: 0.64rem; font-weight: 700;">● Delivered</span></span>
                                <span style="text-align: right; color: var(--uv-emerald); font-weight: 800; font-family: 'Space Mono', monospace;">₹@item.TotalAmount</span>
                            </div>
                        }
                    </div>
                }
            </div>
        </section>

        <!-- ── 7. Quick Actions Section ── -->
        <section class="dash-card-section quick-actions-box">
            <div class="section-header-row" style="margin-bottom: 0;">
                <span style="font-size: 0.88rem; font-weight: 800; color: #ffffff;">Quick Actions</span>
                <span style="color: var(--uv-emerald); font-size: 0.62rem; font-family: 'Space Mono', monospace; font-weight: 700;">✦ Shortcuts</span>
            </div>

            <!-- Wide Create New Request Button -->
            <a asp-controller="Dashboard" asp-action="NewRequest" class="btn-create-request-big">
                <i class="bi bi-plus-lg"></i>
                <span>Create New Request</span>
            </a>

            <!-- Action Rows Stack -->
            <div style="display: flex; flex-direction: column; gap: 0.35rem;">
                <a asp-controller="Dashboard" asp-action="Requests" class="shortcut-item-row">
                    <div class="shortcut-item-left">
                        <i class="bi bi-file-earmark-text"></i>
                        <span>My Requests</span>
                    </div>
                    <i class="bi bi-chevron-right shortcut-chevron"></i>
                </a>

                <a asp-controller="Dashboard" asp-action="Runner" class="shortcut-item-row">
                    <div class="shortcut-item-left">
                        <i class="bi bi-bicycle"></i>
                        <span>Runner Mode</span>
                    </div>
                    <i class="bi bi-chevron-right shortcut-chevron"></i>
                </a>

                <a asp-controller="Dashboard" asp-action="Wallet" class="shortcut-item-row">
                    <div class="shortcut-item-left">
                        <i class="bi bi-wallet2"></i>
                        <span>Wallet</span>
                    </div>
                    <i class="bi bi-chevron-right shortcut-chevron"></i>
                </a>

                <a asp-controller="Dashboard" asp-action="Chat" class="shortcut-item-row">
                    <div class="shortcut-item-left">
                        <i class="bi bi-chat-dots"></i>
                        <span>Chat Support</span>
                    </div>
                    <i class="bi bi-chevron-right shortcut-chevron"></i>
                </a>

                <a asp-controller="Dashboard" asp-action="Requests" asp-route-tab="analytics" class="shortcut-item-row">
                    <div class="shortcut-item-left">
                        <i class="bi bi-bar-chart"></i>
                        <span>Analytics</span>
                    </div>
                    <i class="bi bi-chevron-right shortcut-chevron"></i>
                </a>
            </div>
        </section>

        <!-- ── 8. Profile Summary Card ── -->
        <section class="dash-card-section profile-summary-box">
            <div class="profile-top-avatar-row">
                <!-- Large Initial Avatar 'A' -->
                <div class="avatar-initial-box">
                    @Model.Initial
                </div>

                <!-- Student Identity & Verified Badge -->
                <div class="profile-identity-info">
                    <span class="student-full-name">@Model.DisplayName</span>
                    <span class="student-verified-pill">
                        <i class="bi bi-shield-check"></i>
                        <span>Verified Student</span>
                    </span>
                </div>
            </div>

            <!-- Contact Information -->
            <div class="profile-contact-data">
                <div class="contact-data-line">
                    <i class="bi bi-envelope"></i>
                    <span>@Model.Email</span>
                </div>
                <div class="contact-data-line">
                    <i class="bi bi-telephone"></i>
                    <a href="javascript:void(0)" onclick="promptPhone()" style="color: #7f9689; text-decoration: none;">
                        @(Model.PhoneNumber ?? "Add phone number")
                    </a>
                </div>
            </div>

            <!-- Edit Profile Button -->
            <a asp-controller="Dashboard" asp-action="Profile" class="btn-edit-student-profile">
                <i class="bi bi-pencil-square"></i>
                <span>Edit Profile</span>
            </a>
        </section>

        <!-- ── 9. Recent Activity Section ── -->
        <section class="dash-card-section" style="margin-bottom: 1rem;">
            <div class="section-header-row" style="margin-bottom: 0;">
                <span style="font-size: 0.88rem; font-weight: 800; color: #ffffff;">Recent Activity</span>
            </div>

            @if (Model.Activities.Count == 0)
            {
                <div class="empty-placeholder" style="padding: 1.5rem 0.5rem;">
                    No recent activity.
                </div>
            }
            else
            {
                <div style="display: flex; flex-direction: column; gap: 0.65rem; padding-top: 0.4rem;">
                    @foreach (var act in Model.Activities)
                    {
                        <div style="display: flex; gap: 0.55rem; align-items: flex-start;">
                            <div style="width: 20px; height: 20px; border-radius: 50%; background: rgba(0,229,153,0.12); color: var(--uv-emerald); display: flex; align-items: center; justify-content: center; font-size: 0.65rem; flex-shrink: 0; margin-top: 2px;">
                                <i class="bi @act.IconType"></i>
                            </div>
                            <div style="font-size: 0.72rem;">
                                <p style="color: #ffffff; margin-bottom: 0.1rem;">@act.Description</p>
                                <span style="color: var(--uv-text-dim); font-size: 0.64rem;">@act.TimeAgo</span>
                            </div>
                        </div>
                    }
                </div>
            }
        </section>
    </main>

    <!-- ── Interactive Client Scripts: Dynamic Starfield & Actions ── -->
    <script>
        // ── 1. HTML5 Canvas Starfield with Subtle Twinkling matching Next.js ──
        (function initStarfield() {
            const canvas = document.getElementById('starCanvas');
            if (!canvas) return;
            const ctx = canvas.getContext('2d');
            let w = (canvas.width = window.innerWidth);
            let h = (canvas.height = window.innerHeight);

            window.addEventListener('resize', () => {
                w = canvas.width = window.innerWidth;
                h = canvas.height = window.innerHeight;
            });

            const starCount = 60;
            const stars = Array.from({ length: starCount }, () => ({
                x: Math.random() * w,
                y: Math.random() * h,
                r: Math.random() * 1.3 + 0.3,
                alpha: Math.random() * 0.7 + 0.2,
                speed: Math.random() * 0.02 + 0.005,
                phase: Math.random() * Math.PI * 2
            }));

            let t = 0;
            function render() {
                t += 0.02;
                ctx.clearRect(0, 0, w, h);

                stars.forEach(s => {
                    const currentAlpha = s.alpha * (0.6 + 0.4 * Math.sin(t * s.speed * 20 + s.phase));
                    ctx.beginPath();
                    ctx.arc(s.x, s.y, s.r, 0, Math.PI * 2);
                    ctx.fillStyle = `rgba(255, 255, 255, ${currentAlpha})`;
                    ctx.fill();
                });

                requestAnimationFrame(render);
            }
            render();
        })();

        // ── 2. Time-Based Greeting ──
        (function updateGreeting() {
            const hours = new Date().getHours();
            let text = "Good night 🌙";
            if (hours >= 5 && hours < 12) {
                text = "Good morning ☀️";
            } else if (hours >= 12 && hours < 17) {
                text = "Good afternoon 🌤️";
            } else if (hours >= 17 && hours < 21) {
                text = "Good evening 🌇";
            } else {
                text = "Good night 🌙";
            }
            const el = document.getElementById('greetingText');
            if (el) el.innerText = text;
        })();

        // ── 3. Notification Popup ──
        function showNotice(title, message) {
            alert(title + ":\n" + message);
        }

        // ── 4. Phone Number Prompt ──
        function promptPhone() {
            const phone = prompt("Enter your campus mobile / WhatsApp number:");
            if (phone) {
                alert("Phone number saved: " + phone);
            }
        }

        // ── 5. Hamburger Menu Toggle ──
        function toggleNavMenu() {
            window.location.href = '@Url.Action("Index", "Home")';
        }
    </script>
</body>
</html>
